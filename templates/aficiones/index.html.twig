{% extends 'base.html.twig' %}

{% block title %}Aficiones index{% endblock %}

{% block body %}
<div class="contenedor">
    <header>
        <h1>Aficiones</h1>
    </header>
    <main>
        <table class="tabla">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for aficione in aficiones %}
                <tr class="hover-bg">
                    <td>{{ aficione.id }}</td>
                    <td>{{ aficione.Nombre }}</td>
                    <td>
                        <a href="{{ path('aficiones_show', {'id': aficione.id}) }}" class="btn">Mostrar</a>
                        <a href="{{ path('aficiones_edit', {'id': aficione.id}) }}" class="btn">Editar</a>
                        <div class="caja-btn">
                            {{ include('aficiones/_delete_form.html.twig') }}
                        </div> 
                    </td>
                </tr>
            {% else %}
                <tr class="hover-bg">
                    <td colspan="3">No hay información</td>
                </tr>
            {% endfor %}
                <tr>
                    <td><a href="{{ path('aficiones_new') }}" class="btn">Agregar nueva</a></td>
                </tr>
            </tbody>
        </table>
    </main>
</div>
{% endblock %}
