{% extends 'base.html.twig' %}

{% block title %}Aficiones{% endblock %}

{% block body %}
    <h1>Aficiones</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aficione.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ aficione.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aficiones_index') }}">back to list</a>

    <a href="{{ path('aficiones_edit', {'id': aficione.id}) }}">edit</a>

    {{ include('aficiones/_delete_form.html.twig') }}
{% endblock %}
