{% extends 'base.html.twig' %}

{% block title %}Edit Ciudades{% endblock %}

{% block body %}
    <h1>Edit Ciudades</h1>

     {% for message in app.flashes('error') %}
            <div class="alert alert-warning">
                {{ message }}
            </div>
        {% endfor %}

    {{ include('ciudades/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('ciudades_index') }}">back to list</a>

    {{ include('ciudades/_delete_form.html.twig') }}
{% endblock %}
