{% extends 'base.html.twig' %}

{% block title %}Edit Usuarios{% endblock %}

{% block body %}
    <h1>Edit Usuarios</h1>

   

    {{ include('usuarios/_form.html.twig', {'button_label': 'Update'}) }}
    
    {% if usuario.imagen != null %}
          <a href="{{ path('borrar_foto', {imagen: usuario.imagen}) }}">Eliminar Foto</a>          
    {% endif %}
    
    {{dump(usuario)}}
    <a href="{{ path('usuarios_index') }}">back to list</a>

    <?php var_dump(usuario.imagen); ?>

    {{ include('usuarios/_delete_form.html.twig') }}
{% endblock %}
